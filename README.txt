rand by Yoshua-chan

USAGE: rand [options] [arguments]

OPTIONS:
  --help	show this message and exit
  --version	show the program version and exit
  -r		set the number range (default is max int)
  -s		set the array size
  -n		separate the numbers with newline character (space is the default separator)
